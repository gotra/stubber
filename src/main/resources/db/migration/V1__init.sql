
CREATE TABLE  stubs(
id BIGINT GENERATED BY DEFAULT AS IDENTITY,
urlpath VARCHAR(2083),
isActive BOOLEAN DEFAULT TRUE NOT NULL,
isSite BOOLEAN  DEFAULT FALSE NOT NULL,
directoryPath VARCHAR(2000),
httpmethod VARCHAR(50),
headers CLOB,
response CLOB);

